import { PlatformSessionsPanel } from '../components/PlatformSessionsPanel';

/** Dedicated route — sidebar “Sessions”. Panel includes its own heading and table. */
export function PlatformSessionsPage() {
  return <PlatformSessionsPanel />;
}
